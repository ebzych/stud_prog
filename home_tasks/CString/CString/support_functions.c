#include "support_functions.h"
#include "strings_t.h"
#include "initiator_string.h"

size_t count_char_inputs(string_t *string, const char *delim) {
    size_t count = 1;                                   // initialize with 1 because number of 
}