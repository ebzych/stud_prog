#ifndef SUPFUNC
#define SUPFUNC

typedef struct string_s string_t;
typedef struct strings_array_s strings_array_t;
#include <stdlib.h>
#include "string_t.h"

size_t count_char_inputs(const char *string, char delim);

#endif  //SUPFUNC